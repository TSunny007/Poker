package poker;

public enum Value{
	Two, Three, Four, Five, Six, Seven, Eight, Nine, Ten, Jack, Queen, King, Ace;

	//To compare values we will use .Ordinal() method, which returns a number for ordering based on the
	//order that we wrote the numbers
}
